import React from 'react';
import LoginForm from './LoginForm';


this.state ={
 login: false,

}


const LoginPage = (props) => {
  this.setLogin = (onClick, lockin) => {
    this.setState = (login) => {
        this.return (
            <LoginPage login={false} />
        );
    };
};
  return (
    <React.Fragment>
      <h2>Anmelden</h2>
      <LoginForm />
    </React.Fragment>
  )
}

export default LoginPage;